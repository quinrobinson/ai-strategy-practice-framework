# /project:retro

Start an After session — retrospective evaluation.

Load `.aspf/context.json` if it exists. Set `entry_point: "after"`. Run Layer 1 context snapshot, then Branch C intake from `docs/aspf-decision-tree-spec.md`. Primary skill: retrospective. Generate feed-forward block at close.
