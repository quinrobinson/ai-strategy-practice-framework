# /project:predict

Run the Predictor standalone against current context.

Load `.aspf/context.json`. Load `.claude/skills/failure-mode-library/SKILL.md`. Cross-reference all session context against the full failure mode library. Output a Predictive Risk Report: imminent risks (signals present now), emerging risks (conditions forming), and no-evidence list.
