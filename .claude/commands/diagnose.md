# /project:diagnose

Start a Before session — full decision tree from scratch.

Load `.aspf/context.json` if it exists. Set `entry_point: "before"`. Run Layer 1 context snapshot, then Branch A intake from `docs/aspf-decision-tree-spec.md`. Route sequentially through: problem-qualification → ai-method-selection → risk-mapping → outcome-definition → readiness-audit → phase-routing.
