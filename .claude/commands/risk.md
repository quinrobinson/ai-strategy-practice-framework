# /project:risk

Surface the current risk profile for this session.

Load `.aspf/context.json`. Display all active `predictor_flags` with severity and current status. If no flags exist, run a lightweight Predictor sweep against current context and surface any matches.
