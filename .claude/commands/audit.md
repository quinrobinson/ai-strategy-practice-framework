# /project:audit

Start a During session — mid-project recalibration.

Load `.aspf/context.json` if it exists. Set `entry_point: "during"`. Run Layer 1 context snapshot, then Branch B intake from `docs/aspf-decision-tree-spec.md`. Primary skill: mid-project-audit. Pull additional skills in revalidation mode as needed.
